#include<stdio.h>

struct stack
{
    int siz;
    int top;
    int *arr;
};
int main()
{
    int struct stack *s=(int stack*)malloc(sizeof(struct stack));
    s->siz=4;
    s->top=-1;
    s->arr(int*)malloc(s->siz*sizeof(int));

    printf("initially:",stack(s));


}
